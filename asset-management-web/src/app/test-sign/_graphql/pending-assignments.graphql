query GetPendingAssignments($token: String!) {
  getPendingAssignments(token: $token) {
    id
    signatureR2Key
    asset {
      id
      assetTag
      serialNumber
      category {
        name
        picture
      }
    }
    recentSignatureUrl
    recentSignatureKey
  }
}
